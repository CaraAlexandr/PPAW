<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Property for log file location -->
    <property name="LOG_FILE" value="${LOG_FILE:-logs/application.log}"/>
    <property name="LOG_ARCHIVE" value="${LOG_ARCHIVE:-logs/archive}"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender with daily rolling -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>${LOG_ARCHIVE}/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Keep 30 days of logs -->
            <maxHistory>30</maxHistory>
            <!-- Total size cap -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Application-specific loggers -->
    <logger name="com.ppaw.passwordvault" level="INFO"/>
    <logger name="com.ppaw.passwordvault.service" level="DEBUG"/>
    <logger name="com.ppaw.passwordvault.controller" level="INFO"/>
    
    <!-- Spring Framework loggers -->
    <logger name="org.springframework" level="INFO"/>
    
    <!-- Hibernate loggers - SQL queries at DEBUG level -->
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
    
    <logger name="org.flywaydb" level="INFO"/>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>

